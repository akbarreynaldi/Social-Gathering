Draw = (
    id,
    socialGatheringId,
    participantId,
    amount,
    date,
    createdAt,
    updatedAt
) => {
    return {
        id,
        socialGatheringId,
        participantId,
        amount,
        date,
        createdAt,
        updatedAt
    }
}

module.exports = Draw;